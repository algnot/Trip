import React from 'react'

export default function Loading() {
  return (
    <div className="fixed z-50 flex items-center justify-center top-0 left-0 w-full h-full bg-background">
        Loading
    </div>
  )
}
